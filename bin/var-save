#!/usr/bin/env python
# Save a list of settings

from sys  import stdin, argv
from util.vars import process_vars

if len(argv)>1:
    process_vars(stdin.read(), argv[1])
else:
    process_vars(stdin.read())
